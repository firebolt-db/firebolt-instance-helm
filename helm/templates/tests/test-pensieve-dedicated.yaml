apiVersion: v1
kind: Pod
metadata:
  name: {{ include "fbinstance.fullname" . }}-test-pensieve-dedicated
  namespace: {{ .Release.Namespace }}
  annotations:
    helm.sh/hook: test
    helm.sh/hook-delete-policy: before-hook-creation,hook-succeeded
spec:
  restartPolicy: Never
  containers:
    - name: check
      image: busybox:stable
      command:
        - sh
        - -c
        - |
          HOST="{{ include "fbinstance.fullname" . }}-pensieve-dedicated.{{ .Release.Namespace }}.svc.cluster.local"
          echo "Probing PensieveDedicated at $HOST:{{ .Values.metadata.server.port }} ..."
          nc -z -w 10 "$HOST" {{ .Values.metadata.server.port }} \
            && echo "PASS: PensieveDedicated port {{ .Values.metadata.server.port }} reachable" \
            || { echo "FAIL: could not reach PensieveDedicated"; exit 1; }
